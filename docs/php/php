//Создания процесса php с помощью расширения pcntl
$pid = pcntl_fork();
if($pid !== 0){
    pcntl_setpriority(10);
    echo "Parent" . PHP_EOL;
}
else{
    pcntl_setpriority(5);
    echo "Child" . PHP_EOL;
}
